import React from 'react';
import {StylesSheet, TextInput, View} from 'react-native';
import color from '../utils/colors';

export default function Form() {
  return (
    <View>
      <TextInput placeholder="Cantidad a pedir" />
    </View>
  );
}
